#include <stdio.h>


int stat1() {
	double highx, highy, highHeight;
	double lowx, lowy, lowHeight;
	scanf("%lf %lf %lf", &highx, &highy, &highHeight);
	printf("%lf/n",highx);

	return 0;
}

int main() {
	stat1();
	return 1;
}