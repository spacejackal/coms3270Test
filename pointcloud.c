#include <stdio.h>


int stat1(char name[]) {
	double highx, highy, highHeight;
	double lowx, lowy, lowHeight;
	scanf("%f %f %f", &highx &highy & highHeight);
	printf("%d/n",highx);

	return 0;
}

int main() {
	stat1();
	return 1;
}